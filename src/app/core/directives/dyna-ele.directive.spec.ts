import { DynaEleDirective } from './dyna-ele.directive';

describe('DynaEleDirective', () => {
  it('should create an instance', () => {
    const directive = new DynaEleDirective();
    expect(directive).toBeTruthy();
  });
});
